<nav class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="container-fluid d-flex align-items-center">

    <!-- LEFT: Logo -->
    <div class="navbar-left">
      <%= link_to image_tag("insight_loop_logo_reduzido.png", alt: "InsightLoop"),
            home_path,
            class: "navbar-brand" %>
    </div>

    <!-- CENTER: Greeting + Date -->
    <% if user_signed_in? %>
      <div class="navbar-greeting">
        <div class="fw-bold" style="font-size: 1.1rem;">
          Bom dia, <%= current_user.name.presence || current_user.email.split("@").first.titleize %>
        </div>
        <div class="text-muted" style="font-size: 0.9rem;">
          <%= l(Date.today, format: :long).capitalize %>
        </div>
      </div>
    <% end %>

    <!-- RIGHT: Home + Avatar -->
    <div class="navbar-right d-flex align-items-center">
      <% if user_signed_in? %>
        <%= link_to "Home", root_path, class: "nav-link me-2" %>

        <div class="nav-item dropdown">
          <a href="#" class="avatar" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false"
             style="width:36px;height:36px;border-radius:50%;background:#6366f1;color:#fff;display:inline-flex;align-items:center;justify-content:center;font-weight:600;font-size:0.9rem;text-decoration:none;">
            <%= current_user.name&.first&.upcase || current_user.email.first.upcase %>
          </a>
          <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
            <%= link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item" %>
          </div>
        </div>
      <% else %>
        <%= link_to "Login", new_user_session_path, class: "nav-link" %>
      <% end %>
    </div>

  </div>
</nav>
